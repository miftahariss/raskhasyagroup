/** * @author Rifki */ function validateInput() {    var obj     = document.addArticle;    var channel = trim(obj.channel.value);    var title 	= trim(obj.title.value);    var body 	= trim(obj.body.value);    var message = "";    if (title == '') { message = message + "\n - Title harus diisi"; }    if (body == '') { message = message + "\n - Body harus diisi"; }    if (channel == 'pilih') { message = message + "\n "; }    if (message != "") {        alert("Alert List : " + message);    } else {        obj.action = "<?php echo base_url();?>acladmin/add_article";        obj.submit();    }}function trim(item) {    var tmp                 = "";    var item_length         = item.length;    var item_length_minus_1 = item.length - 1;    for (index = 0; index < item_length; index++) {      if (item.charAt(index) != ' ') {          tmp += item.charAt(index);      } else {        if (tmp.length > 0) {          if ((item.charAt(index+1) != ' ') && (index != item_length_minus_1)) {            tmp += item.charAt(index);          }        }      }    }    return (tmp) ;}